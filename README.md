# Experiment sana2b-cache_ca-0001

This worktree was created from model contract
`models/sana_video_2b_h100/model.toml` using minimal baseline copy mode.

It contains the baseline runnable closure for model `sana_video_2b_h100`
and is owned by workflow `cache_ca`. It intentionally does
not contain historical runs, generated config, or other workflow search
spaces unless a workflow creates them inside this experiment.
